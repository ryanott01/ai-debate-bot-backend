# AI Microservice Configuration
# Copy this file to .env and replace with your actual values

# ===== API Keys =====
# You only need to provide keys for the providers you want to use
# Leave empty or comment out providers you don't want to use

# OpenAI API Key
# Required for OpenAI provider (GPT models)
OPENAI_API_KEY=

# Anthropic API Key
# Required for Anthropic provider (Claude models)
ANTHROPIC_API_KEY=

# Google API Key
# Required for Google provider (Gemini models)
GOOGLE_API_KEY=

# ===== Flask Configuration =====
# These settings are optional and provide defaults for Flask

# Main application file
FLASK_APP=app.py

# Development environment (use 'production' for production)
FLASK_ENV=development

# Enable debug mode (1 for enabled, 0 for disabled)
FLASK_DEBUG=1

# ===== Server Configuration =====

# Port to run the server on (default is 4000)
# Note: On macOS, port 5000 is used by AirPlay, so we use 4000 as default
PORT=4000
